# Use the official Caddy image, which is lightweight and secure.
FROM caddy:2-alpine


# Use a mounted Caddyfile instead (provided by the build context or volume)

# Caddy automatically exposes ports 80 and 443.
# The docker-compose file will handle the networking.
